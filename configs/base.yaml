run:
  output_root: runs

paths:
  raw_dir: data/raw
  intermediate_dir: data/intermediate
  results_dir: results

fetch:
  dataset_name: bike_rental_demand
  raw_artifact_name: bike_demand_raw.csv
  source_data_path: data/source/bike_demand_source.csv
  source_url: https://archive.ics.uci.edu/ml/machine-learning-databases/00275/Bike-Sharing-Dataset.zip

prepare:
  prepared_table_name: prepared_demand.csv

analysis:
  # Student task:
  # Fill these values in here and in `configs/stages/analyze.yaml`.
  # Final output names should be:
  # - dataset_overview.json
  # - hourly_demand_profile.csv
  # - weekday_weekend_daily_cycle.png
  dataset_overview_name: null
  hourly_profile_name: null
  daily_cycle_plot_name: null
  generate_daily_cycle_plot: null

report:
  # Student task:
  # Fill these values in here and in `configs/stages/report.yaml`.
  # Final output name should be `report.md`.
  markdown_name: null
  include_daily_cycle_plot: null

profile:
  name: base
