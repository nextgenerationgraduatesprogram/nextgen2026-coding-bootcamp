run:
  output_root: runs

paths:
  raw_dir: data/raw
  intermediate_dir: data/intermediate
  results_dir: results

fetch:
  dataset_name: sms_spam_collection
  uci_dataset_id: 228
  raw_artifact_name: sms_spam_collection.tsv
  force_download: false

prepare:
  prepared_messages_name: prepared_messages.csv

analysis:
  # Student task:
  # Fill these values in here and in `configs/stages/analyze.yaml`.
  # Final output names should be:
  # - message_predictions.csv
  # - evaluation_summary.json
  predictions_name: null
  evaluation_summary_name: null
  sample_size: null
  sample_seed: null
  # Keep the model name in config. The OpenAI API key is loaded from `.env`.
  model: null
  temperature: null

report:
  # Student task:
  # Fill these values in here and in `configs/stages/report.yaml`.
  # Final output name should be `report.md`.
  markdown_name: null
  max_examples: null

profile:
  name: base
